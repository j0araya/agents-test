# Production override — backend only, no local mongo.
# Reads .env.prod from the repo root (gitignored).
# MONGODB_URI must point to MongoDB Atlas.
# Usage: docker compose -f docker-compose.yml -f docker-compose.prod.yml up --build

services:
  backend:
    env_file: ./backend/.env.prod
