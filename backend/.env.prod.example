# Production environment — NEVER commit this file.
# Copy from .env.prod.example and fill in real values.

DB_USER=claude
DB_PASS=S6rx58WuvM8HtSK5
DB_NAME=chile_atlas
DB_HOST=chile.l6dhgho.mongodb.net

# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://${DB_USER}:${DB_PASS}@${DB_HOST}/${DB_NAME}?retryWrites=true&w=majority

# Server
PORT=3001

# CORS — set to your production frontend URL
CORS_ORIGIN=localhost:3000
